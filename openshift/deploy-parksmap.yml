---
- name: Create parksmap instance in OpenShift
  hosts: all
  user: ec2-user
  
  tasks:
  - name: Invoke shell script
    shell: /home/ec2-user/scripts/dockerimage.sh

  - name: tail file
    command: tail -1 /home/ec2-user/parksmap
    register: url

  - name: show url
    debug:
      var: url.stdout
...
