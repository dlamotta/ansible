---
- name: OpenShift Route Creation
  hosts: localhost
  connection: local
  gather_facts: False
  vars:
    namespace: ns
    name: name
    hostname: url
    token: ocptoken
    host: ocpapi
  
  tasks:  
  - name: Create a route
    oc:
      state: present
      host: "{{ host }}"
      validate_certs: False
      inline:
        kind: Route
        metadata:
          name: "{{ name }}"
          namespace: "{{ namespace }}"
          hostname: "{{ hostname }}"
      token: "{{ token }}"
