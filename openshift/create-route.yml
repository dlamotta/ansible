---
- name: OpenShift Route Creation
  hosts: localhost
  connection: local
  vars:
    namespace: ns
    route: name
    token: ocptoken
    host: ocpapi
    
  tasks:
  - name: Create a route
    oc:
      host: "{{ host }}"
      state: present
      name: "{{ route }}"
      namespace: "{{ namespace }}"
      kind: Route
      token: "{{ token }}"
