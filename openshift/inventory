[masters]
ocp-m1    provider=openstack     region=infra      zone=default

[nodes]
ocp-n1    provider=openstack     region=primary    zone=east

# Uncomment if a new node is needed in the cluster
[newnodes]
ocp-n2    provider=openstack     region=primary    zone=east     ocp-master=ocp-m1
